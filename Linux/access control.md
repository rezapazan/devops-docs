# Access Control

important files:

- /etc/shadow
- /etc/passwd
- /etc/group
- /etc/sudoers

## Commands

```bash
$ adduser
$ userdel
$ passwd
# change password of user
$ last
# shows last logins
$ id
# shows ids of user
$ w
# shows current sessions
```
